<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- #################################### -->
    <!-- ########### PERMISSIONS ############ -->
    <!-- #################################### -->

    <!-- INTERNET and Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    
    <!-- Required for Local Notifications -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    
    <!-- Required for Download, File Access & Management -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    
    <!-- Required for Camera Access (if using camera features) -->
    <uses-permission android:name="android.permission.CAMERA"/>
    
    <!-- Required for Download Manager and Background Services -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    
    <!-- Required for accessing external files in Android 10+ -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    
    <!-- Required for notifications and alarms -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- #################################### -->
    <!-- ########## APPLICATION ############ -->
    <!-- #################################### -->
    
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:label="Zen Active"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- #################################### -->
        <!-- ########## MAIN ACTIVITY ########## -->
        <!-- #################################### -->
        
        <activity android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:enableOnBackInvokedCallback="true">

            <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- #################################### -->
        <!-- ##### FLUTTER LOCAL NOTIFICATIONS ##### -->
        <!-- #################################### -->
        
        <receiver android:name="com.dexterous.flutterlocalnotifications.receivers.NotificationReceiver" android:exported="true"/>
        <receiver android:name="com.dexterous.flutterlocalnotifications.receivers.ScheduledNotificationReceiver" android:exported="true"/>
        <receiver android:name="com.dexterous.flutterlocalnotifications.receivers.ActionReceiver" android:exported="true"/>

        <!-- #################################### -->
        <!-- ###### FILE PROVIDER FOR FILE ACCESS ###### -->
        <!-- #################################### -->
        
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

        <!-- #################################### -->
        <!-- ###### FLUTTER DOWNLOADER ###### -->
        <!-- #################################### -->

        <!-- Downloader Provider (Flutter Downloader) -->
        <provider
            android:name="vn.hunghd.flutterdownloader.DownloadedFileProvider"
            android:authorities="${applicationId}.flutter_downloader.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

        <!-- WorkManager Configuration: Max Concurrent Tasks -->
        <!-- Disable default Initializer -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove"/>
        </provider>

        <!-- Declare Custom Downloader Initializer -->
        <provider
            android:name="vn.hunghd.flutterdownloader.FlutterDownloaderInitializer"
            android:authorities="${applicationId}.flutter-downloader-init"
            android:exported="false">
            <!-- Change this value to configure max concurrent download tasks -->
            <meta-data
                android:name="vn.hunghd.flutterdownloader.MAX_CONCURRENT_TASKS"
                android:value="5"/>
        </provider>

        <!-- #################################### -->
        <!-- ####### FLUTTER AUTO-GENERATED ####### -->
        <!-- #################################### -->
        <meta-data android:name="flutterEmbedding" android:value="2"/>

    </application>

    <!-- #################################### -->
    <!-- ####### TEXT PROCESSING QUERY ####### -->
    <!-- #################################### -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
